#include <table/tablependialog.h>

TableColorDialog::TableColorDialog(QWidget*parent):QDialog(parent)
{
    mainLayout = new QGridLayout;
    mBackLabel= new QLabel(tr("选择前景色: "));
    mForeLabel = new QLabel(tr("选择背景色: "));
    mOkBtn = new QPushButton(tr("确定"));
    mCancelBtn = new QPushButton(tr("取消"));
}
